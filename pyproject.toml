[project]
name = "aiml-agentic-world-cup-predictor"
version = "0.1.0"
description = "Agentic AI World Cup football prediction project"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]